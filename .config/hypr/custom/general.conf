# Put general config stuff here
# Here's a list of every variable: https://wiki.hyprland.org/Configuring/Variables/
# Personal overrides live here so end4/Hyprland updates to hyprland/general.conf
# can't revert them (this file is sourced last and wins).

general {
    gaps_in = 0
    gaps_out = 0
    border_size = 0
    col.active_border = rgba(0DB7D455)
}